# Previz Studio v1.3.0 — 사용자 매뉴얼

## 1. 장면 만들기
하단 입력창에 지원되는 표현을 입력하고 `장면 만들기`를 누릅니다.

예시:

> 두 사람이 격렬하게 하는 격투씬, 카메라가 다양한 각도로 익사이팅한 앵글로 따라간다.

이 표현은 기본적으로 두 배우의 FIGHT 액션과 여러 카메라 각도의 20초 시퀀스로 블로킹됩니다.

## 2. 편집 뷰
편집 뷰는 자유 시점입니다. 배우 동선, 카메라 경로, Transform Gizmo를 확인합니다. 최종 출력 화면은 `카메라 프리뷰`에서 확인합니다.

## 3. 카메라 편집
오른쪽 `Transform` 섹션에서 `카메라`를 선택합니다.

- 키: 시작 / 끝
- 이동: X/Y/Z 또는 Gizmo
- 높이: Y 높이를 직접 변경
- 거리: 현재 타겟과 카메라 사이 거리를 변경
- 타겟: 자유 포인트 / 배우 / 두 배우 중점
- 타겟 오프셋: 화면에서 바라보는 높이와 위치를 조정

수정한 Shot은 `사용자 수정` 상태가 됩니다. `자동 구도로 되돌리기`를 누르면 원래 자동 구도로 돌아갑니다.

## 4. Transform Gizmo
- W: 이동
- E: 회전
- T: 타겟 편집

카메라에서 T는 target point를 움직입니다. Actor에서는 이동/회전을 사용합니다.

## 5. Actor 편집
Actor를 선택한 뒤 이동 또는 회전합니다. Actor 이동은 해당 배우의 전체 Action 경로를 함께 이동시켜 타임라인 동작을 유지합니다.

## 6. 카메라 프리뷰
`카메라 프리뷰`는 16:9 Canonical Frame입니다. Preview에서 보이는 구도는 PNG/영상 출력과 동일한 렌더 경로를 사용합니다.

## 7. 영상 출력
`프리비즈 영상 렌더`를 누르면 1920×1080 / 24 FPS / 프로젝트 길이 기준으로 출력합니다.

## 8. 지원되는 자연어 범위

v1.3.0은 복잡한 의미 추론 대신 다음과 같은 표현을 규칙으로 추출합니다.

- 인원: 한 사람 / 두 사람 / 두 명 / 세 명 / 네 명
- 액션: 걷기 / 달리기 / 추격 / 격투·싸움
- 카메라: 따라가기 / 오비트 / 푸시 / 핸드헬드 / 다양한 각도 / 역동적 / 익사이팅
- 렌즈: `24mm`, `35mm`, `50mm` 등
- 길이: `20초` 등

지원되지 않는 세부 묘사는 기본값으로 처리하거나 무시합니다.
